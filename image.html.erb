<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="robots" content="noindex, nofollow, noarchive, nosnippet">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="viewport" content="minimal-ui, width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=2">
  <title><%= image.label %></title>
  <link href="<%= image.path_to_root %>style.css" rel="stylesheet">
  <link href="<%= image.path_to_root %>fontawesome/css/fontawesome.css" rel="stylesheet" />
  <link href="<%= image.path_to_root %>fontawesome/css/solid.css" rel="stylesheet" />
</head>
<body>

<div class="breadcrumbs"><%= image.generate_breadcrumbs %></div>

<div class="image">
<img src="<%= image.main_local_image_path %>">
</div>

<% unless image.caption.empty? %>
  <div class="image-caption">
<h3><%= image.caption %></h3>
  </div>
<% end %>

<% if image.date_time_original %>
<div class="capture-date">
  <p>Date Captured: <%= DateTime.xmlschema(image.date_time_original[0]) %></p>
</div>
<% end  %>

<%= image.image_links %>

<% unless image.keywords.empty? %>
<div class="keywords">
  <h4>Keywords</h4>
  <ul>
<% image.keywords.each do |keyword| %>
    <li><%= keyword %></li>
<% end %>
  </ul>
</div>
<% end %>

</body>
</html>


